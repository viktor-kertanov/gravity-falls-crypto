from big_cipher import text
from ceasar import initial_alphabet
import re

def biggest_substrings(input: str) -> dict[str]:
    input = list(input.lower().strip())
    cur_idx = 0
    final_dict = {}
    for l_idx, l in enumerate(input):
        substrin

    
    return


if __name__ == '__main__':
    biggest_substrings(text)
    print('Hello world!')